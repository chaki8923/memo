めも２です
 Update Bash
-------------
`nb` works best with the current version of Bash.

`nb` can update your system to Bash 5 via Homebrew
or you can proceed with Bash 3.2.

Answering y or yes will run the following command:

  brew install bash

This command could take a minute to run.
